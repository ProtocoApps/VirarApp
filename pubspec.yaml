name: virar_app_android
description: VIRAR - Soluções que Transformam o Cuidado

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.4.0
  provider: ^6.1.1
  
  # Navigation
  go_router: ^12.1.0
  
  # HTTP & Networking
  dio: ^5.3.0
  supabase_flutter: ^2.3.0
  
  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.0
  
  # Security
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.1.6
  
  # UI Components
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  
  # Maps & Location
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  geocoding: ^2.1.0
  
  # Utilities
  equatable: ^2.0.5
  uuid: ^4.2.1
  intl: ^0.20.2
  url_launcher: ^6.2.0
  app_settings: ^5.1.1
  
  # Image Picker
  image_picker: ^1.0.4
  
  # Rating
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
    - assets/images/
    - assets/images/NOME COMPLETO.gif
    - assets/images/CEP.gif
    - assets/images/BAIRRO.gif
    - assets/images/CIDADE.gif
    - assets/images/COMPLEMENTO.gif
    - assets/images/SENHA.gif
    - assets/images/CRIAR SENHA.gif
    - assets/images/CONFIRMAR SENHA.gif
  
  fonts:
    - family: Poppins
